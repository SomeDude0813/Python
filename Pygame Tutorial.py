import pygame
from pygame.locals import *

running = True

# while running:
#     events() # Events like pressed keys, mouse motion, etc.
#     loop() # Compute changes in the game world (Ex: player movement)
#     render() # Print out the screen graphics

class App:
    def on_execute(self):
        pass

if __name__ == "__main__" :
    Game = App()
    Game.on_execute()